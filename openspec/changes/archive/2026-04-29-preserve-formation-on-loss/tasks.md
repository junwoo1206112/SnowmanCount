# Tasks: Preserve Formation on Unit Loss

- [x] 1. `CrowdController.cs` 분석: `RedistributeAngles()`가 호출되는 모든 지점 확인
- [x] 2. `RemoveLastFollower()`에서 `RedistributeAngles()` 호출 제거
- [x] 3. `RemoveSpecificFollower()`에서 `RedistributeAngles()` 호출 제거
- [x] 4. `RemoveFromList()`에서 `RedistributeAngles()` 호출 제거
- [x] 5. 유니티에서 유닛이 죽을 때 남은 유닛들의 위치가 고정되는지 확인

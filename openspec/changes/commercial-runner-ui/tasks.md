## 1. UI implementation

- [ ] 1.1 Update `UICounter` to support badge-style count text and ready prompt hiding.
- [ ] 1.2 Keep `UIProgressBar` compatible with the new progress fill hierarchy.
- [ ] 1.3 Add an editor builder that constructs the commercial runner Canvas in `GameScene`.
- [ ] 1.4 Run the builder through Unity CLI and save the scene.

## 2. Verification

- [ ] 2.1 Check Unity console errors.
- [ ] 2.2 Run PlayMode tests or report if the Unity CLI environment blocks them.
